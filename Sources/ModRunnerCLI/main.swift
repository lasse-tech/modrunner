import ModRunnerCommands

// The console build. Everything it does is in `CLI`, which the windowed build
// wraps the same way; the difference between the two is the subsystem they are
// linked for, not the code.
CLI.main()
